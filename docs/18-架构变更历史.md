# 18 · 架构变更历史

> 精简 changelog，收录已完成的架构/性能变更。完整实验数据在对应 commit 的 diff 与
> 当时的测试输出中；本文只保留动机、方案摘要和结论，不逐字保留原始报告的过程细节。
> 后续架构变更继续在本文追加新的一节，不再新建编号文档。

## 2026-09-01 · 帧特征缓存修复

**问题：**
1. 训练循环未使用 `frame_cache`：持久缓存保存的是 `detach().cpu()` 特征，若训练跨 step 复用会切断视觉投影和条件投影的 autograd 图。
2. 默认评估/诊断抽样命中率接近 0：均匀抽样破坏了滑窗的相邻性。
3. Cache key 缺少 instruction/mode：同一图片路径在不同任务下可能被静默复用错误特征。

**修复：**
- `encode_batch()` 新增 batch 内缓存，复用图连接张量，持久 `frame_cache` 只在 `eval()+no_grad()` 路径使用；
- 新增 `_contiguous_subset()`，评估/诊断改用按 episode 连续抽样；
- 持久缓存键统一为 `(instruction, mode_id, path)`。

**验证：** 新增 `tests/test_frame_cache_behavior.py`；全量 pytest 通过（1 项既存无关失败）。

## 2026-09-02 · 批量视觉与增量特征缓存

**目标：** 降低视觉塔重复编码成本。当时统计全部 `vla_chunks_v4.jsonl`：26,402 条样本引用 211,216 帧，唯一图片仅 27,305（重复率 87%），视觉塔前向理论可减少约 7.74 倍。

**方案：**
- `Qwen3VLBackboneAdapter` 新增批量视觉路径（`encode_frames` + micro-batch），`train_vla.py` 增加 `--vision-micro-batch-size`；
- 新增冻结 raw feature 增量缓存（`idv_agent/training/raw_feature_cache.py`）：只缓存 Qwen 视觉塔 pooling 后的 raw feature（`mean_dim1_v1`），训练时查表后重新执行可训练投影，不产生 stale feature 或梯度断开；
- 新增预编码脚本 `idv_agent/scripts/precompute_vla_features.py`，按 session 增量追加 shard。

**验证：** raw cache 与实时路径逐元素 `torch.equal` 一致；`tests/test_frame_cache_behavior.py` + `tests/test_train_vla_inheritance.py` 17 passed。

**已知限制：** `evaluate_vla.py` 独立 CLI 当时未接入 `--raw-feature-cache`（仍用旧 feature cache）；换模型/processor 需要新建缓存目录，不能混用旧 shard。

## 2026-09-03 · 分离时序编码器（消除梯度冲突）

**动机：** `SharedFastSlowVLA` 原架构中，慢头（intent/subgoal 离散分类）和快头（move/camera/buttons 连续控制）共享同一个 `SharedTemporalEncoder`（GRU），两个损失的梯度同时作用在共享参数上，方向不一致时相互干扰。实测验证集慢头意图准确率仅 16.41%，快头在不同采样策略下波动剧烈（非停止移动准确率 0%→61.89%），怀疑是共享编码器被快头优化主导。

**方案：** `idv_agent/model/fast_slow_vla.py` 的 `SharedFastSlowVLA` 拆分为独立的 `slow_temporal` 和 `fast_temporal` 两个 `SharedTemporalEncoder` 实例；`SlowVLAHead` 只消费 `slow_temporal` 输出，`FastVLAHead` 只消费 `fast_temporal` 输出。公开接口（`__init__`/`forward`/`initial_condition`/`condition_from_slow_output`）签名不变，调用方（`train_vla.py`、`act_policy.py`）无需修改。

**成本：** 额外参数约 1.18M（512-dim GRU × 2），相对 Qwen3-VL-4B + 视觉投影总量增加 <0.03%；GRU 前向 <2ms，推理延迟影响 <5%，仍满足 15 Hz 预算。

**只解决了一半：** 分离 GRU 消除了两个头对**同一份循环权重**的竞争，但 FiLM 条件融合（`context_embedding → FiLMConditioner`）发生在 slow/fast 分叉**之前**，是两者共用的输入路径。`train_vla.py` 当前训练用的两遍前向（第一遍 Slow 产出 `context_embedding`，第二遍用 `detach_slow_condition=False` 把它喂给 Fast）下，快头损失仍会经这条 FiLM 路径回传进 `slow_head`/`slow_temporal`——已用 `tests/test_two_pass_training_scheme_still_couples_fast_loss_into_slow_head` 实测确认（`slow_temporal`/`slow_head` 梯度非零）。这个耦合是有意保留的（否则快头学不到"如何使用慢头给出的上下文"），可通过 `detach_slow_condition=True` 关闭；另一个测试 `test_single_pass_gradient_flow_is_independent_when_condition_is_fixed` 确认了单次前向、条件为固定常量时两个 GRU 完全独立。

**验证：** `tests/test_separated_temporal.py`（4 项）覆盖参数独立性、输出形状、单次前向的梯度独立性、以及两遍训练下 FiLM 耦合的真实存在与可关闭性；已有 `test_act_policy.py`（5 项）与 `test_configs.py` 中 VLA 相关测试全部通过。

**破坏性影响：** 旧 checkpoint 字段 `core.temporal.*` 不兼容新的 `core.slow_temporal.*`/`core.fast_temporal.*`，`M3_ACT` 需要从 `M2_VG` 重新训练。

**后续验证项（未完成）：**
1. 用新架构重跑一次正式训练，对比慢头验证准确率是否从 16% 明显提升；
2. 若仍不收敛，尝试在第二遍前向显式传 `detach_slow_condition=True`（彻底切断 FiLM 耦合）做对照实验，判断残留耦合是否仍是主因；
3. 如果分离 GRU + 关闭 FiLM 耦合后依然不收敛，说明问题不止梯度冲突，需回到采样/数据质量排查。

## 2026-09-04 · VLA 数据校验 fail-closed

**问题：** raw 校验只覆盖了鼠标 CSV 的部分字段；动作构建对缺失、非法数值和缺失帧使用默认零值。坏标签因此会进入 v4 JSONL，表现为大量 stop/no-camera 样本并诱发模型坍塌。旧 audit 也没有逐条验证 v4 schema 或动作跨度。

**修复：**
- `validate_vla_raw.py` 统一校验三份 CSV 的表头、数值、时间顺序、帧文件 ID、meta 帧数/FPS/录制窗口；允许同时间戳 Raw Input 事件，但拒绝超出 `meta.end_ts_ns` 的事件；
- `build_vla_chunks.py` 要求 `per_frame_actions.csv` 覆盖全部帧、无重复、数值有限且时间戳与 frame timestamps 对齐，删除缺失动作补零路径；
- v4 alignment 增加必需的 `action_end_frame`；audit 逐条运行 schema 校验，检查动作跨度，并输出 move/camera/button/零动作分布；
- 新增 raw、chunk、audit 回归测试。

**现有数据扫描：** 100 个 raw session 全部通过增强校验。99 个现有 v4 文件共 7,501 条记录，schema 字段本身可解析，但全部是旧格式，缺少 `alignment.action_end_frame`，必须从 raw session 重新构建后才能进入新训练入口。当前动作分布仍严重偏斜：MVP train/val 的有效动作主要是 north/stop，多个方向为零，button 几乎只有 interact；这属于数据覆盖问题，不能由校验器自动修复。

## 2026-09-04 · spatial k=8 训练/缓存链路收敛

- raw 视觉特征统一为 `[N, k*k, D]`（默认 k=8），不再提供 mean-pooled raw 路径；`SpatialAgg` 在压缩前加入可训练的固定网格位置参数。
- raw cache 版本为 `spatial_grid_k8_v1`，保存 `[64,D]` cell token；spatial ACT 拒绝旧 mean cache。`precompute_vla_features --verify` 可对全量帧重编码并执行逐帧 `torch.equal` 校验。
- M3 checkpoint 保存/恢复 `spatial_agg`，实时 encode 与 cache 均通过同一 `project_raw_features` 聚合路径。

**验证：** `python -m pytest -q --basetemp=tmp/pytest_temp`：132 passed, 1 skipped；`python -m idv_agent.scripts.smoke_test`：通过。

## 2026-09-04 · 分层活性测试定位 mean-pool 为画面信息瓶颈（C 档阶段一）

**问题复现：** 对同一批差异大画面（实测逐像素平均差 37~43/255），`probe_feature_activity.py` 分层抓特征显示：Qwen 视觉塔 **raw pooled**（`.mean(dim=1)` 后的单向量）画面间 cos 已 ≈0.999925（残差 ~2.5e-2 信号能量中只占 7.5e-5），视觉塔每帧上千 patch token 里含的东西/人/密码机位置信息在进入主干前就被整图平均抹平。随后 visual_projection 保留、FiLM/共享 GRU 再在该贫化输入上继续压缩，主踩塌点在 FiLM→GRU 段。

**证据（git log）：** mean-pool 自 `bc9315f`（"Add Qwen frame encoding"，2026-09-02 首次引入单帧视觉编码）起即存在；`codex/vla-action-chunks` 与 `vla-train` 两分支、缓存引入前后、共享/分离 GRU 前后，该 pooling 从未被修改。即**画面空间信息丢失是自引入帧编码就存在的架构级限制，不是某次回归**。

**为什么不做立刻修（取舍）：** 全量保留 patch token 会把单向量帧特征与下游 FiLM/GRU、`FrameFeatureCache`、train/checkpoint 契约全换成 token 序列，现存 M2_VG/M3_ACT 全部失效，需级联重训（M2_VG 的 1024→2560 投影契约也不可复用）。故采用**两阶段**：先只验"保留空间是否真有增益"，再决定是否破链重训。

**阶段一产出（本层诊断扩展）：**
- `idv_agent/model/spatial_grid_feature.py`：冻结视觉塔 per-token → 固定 **k×k 胞 average-pool**（保胞序位置，输出 `[N, k*k, D]`），不改现役 mean 链路，不持可训权重，CPU 可运行；产物连同 0 改动现役契约。
- `idv_agent/scripts/probe_spatial_vs_pooled.py`：同一 adapter 下对照现役 pooled vs k×k 保胞的画面可分性。
- `tests/test_spatial_grid_feature.py`：纯 CPU 几何/胞均值/非法网格/多 tile 反推单元测试。

**测量/结论：** 对 3 张差异大截图，画面可分残差从 pooled 基线 `7.5e-5` → k=4 `3.85e-3`（51x）→ k=8 `1.14e-2`（151x）；`cellmean`（去掉胞位置仍跨胞平均）回复到 0.999925，证明提升来自**保胞序的空间分量**本身，而非单纯维度或均化错误。k=8 展平确实显著拉开画面差异 → **支撑进入阶段二**（把 k×k 保胞 token 接到投影/时序，届时现役 M2_VG/M3_ACT 投影契约与 checkpoint 将按方案破坏并级联重训）。

**caveat（写入报告）：** 展平向量维度 k²×D 自身抬高画面可比性；结论以"保胞位置让 separable 信息进入主干"为据，最终是否对下游行为有增益，仍需阶段二接入投影/GRU 后用原分层活性探测在 GRU 输入端复测。

## 2026-09-04 · v5 采样去相关与训练图像增强

**动机：** 旧的连续 anchor 采样让相邻 chunk 高度重复，模型可以依赖上一动作和 history 的自相关取得较低 loss，削弱当前画面的边际贡献；训练阶段还可能通过固定画面统计建立捷径。

**方案：** v5 将相邻样本 anchor 间隔与观测窗口内部的历史帧间隔解耦：`anchor_stride=12`、`history_stride=3`、`history=8`，动作块仍为 4 步、每步 6 帧。标签不变，只改变 anchor 采样位置；部署与训练仍使用同源的像素相机桶/代表值表。训练 forward 对完整 history window 共享一组增强参数，加入 hue ±0.05、saturation/brightness/contrast 0.8～1.2 和平移 ±8 px；eval、raw session 与 v5 JSONL 不增强。增强与冻结 raw feature cache 互斥，避免缓存污染。

**数据结果：** 当前 99 个 raw session 全部重建并通过 v5 audit，共 1,917 chunks / 7,668 actions；MVP train 为 1,537 chunks（79 sessions），val 为 380 chunks（20 sessions），session 零重叠且两侧均覆盖 `travel`/`decipher`。批量报告位于 `data/new_vla_raw_sessions/vla_chunks_v5.audit_summary.json`。

**已知限制：** 采样和增强不能替代类别平衡。动作仍偏向 stop/north，camera dx=0 仍是多数类；后续训练必须继续观察 image/history A/B、各 head macro-F1、per-class recall 与预测分布，并使用全局类别权重或分桶采样评估是否缓解坍塌。

## 2026-09-04 · 画面依赖四组硬性验收

**动机：** 单看 normal 验证集 loss/accuracy 不能证明模型使用了当前画面；模型可能依赖 history、动作先验或意图多数类取得较低 loss。

**方案：** 新增 `idv_agent/scripts/evaluate_visual_dependency.py`，对每个 M3_ACT checkpoint 使用完全相同的样本、标签、顺序和部署式 slow→fast 推理，固定执行四组：`normal`（真实图像+正常 history）、`image_zero`（图像置黑）、`image_shuffle`（样本级循环替换图像，history 不变）、`history_zero`（清空历史动作，图像不变）。四组各自使用独立视觉缓存，避免退化条件污染 normal 特征。

**放行规则：** `image_zero` 与 `image_shuffle` 必须分别让 `move_accuracy`、`camera_accuracy`、`intent_accuracy` 相对 normal 下降至少 `max(0.05, normal × 0.15)`；任一指标缺失、未下降或下降不足，报告 `visual_dependency_gate_pass=false`，CLI 返回退出码 2。`history_zero` 只作历史依赖诊断，不能替代画面依赖验收。

**输出：** 报告 schema 为 `vla.visual_dependency_gate.v1`，包含四组指标、逐指标 delta/threshold/failure 和每个 checkpoint 的放行结果；批量 checkpoint 只有全部通过才返回 0。该工具为进入 dry-run 的前置硬门禁。

## 2026-09-04 · ACT v5 训练—部署输入协议收敛

**问题：** 训练数据虽使用 8 帧、帧间隔 3 的视觉窗口，但旧实机默认只积累连续 3 帧；训练 history 是单帧摘要，而执行器 history 是 6 帧宏动作；训练的 `time_deltas` 为零，运行时曾可注入真实负时间差。更严重的是，即使缓存容量扩到 22 帧，ready 门槛仍是 8 帧，实机可能用不足 8 个真实采样点开始预测。这些偏差会使离线指标不能代表部署行为。

**协议与修复：** v5 固定为最近 8 帧、视觉 stride=3（缓存/冷启动门槛 22 采集帧）、最近 8 个已执行的 6 帧宏动作、`time_deltas=None`、30 FPS、4×6 帧动作块。`FrameFeatureCache.window()` 支持 stride 抽样且始终保留最新帧；构建器将 v5 history 改为同源的 6 帧 Raw Input 像素净位移宏摘要；运行时固定每步 duration=6，不再使用未经该协议训练的 duration head。v5 校验器和构建/部署入口 fail-closed 拒绝非 8 帧、非 8 history、非 stride=3、非 macro=6、非 30 FPS 或试图注入 time deltas 的配置。

**相机一致性：** 标签继续以 6 帧 Raw Input 像素净位移分桶，执行器继续通过同一个 `camera_command_pixels()` 代表值表发送 `±25/±110 px`，不存在 legacy scale 参与 ACT v5 的路径。

**影响：** 既有 M3_ACT checkpoint 均在旧输入协议下训练，不得据此判断新架构或新数据的质量；后续训练需从基础 Qwen 初始化并在锁定的 v5 数据协议上重新运行，再执行四组画面依赖验收。

## 2026-09-04 · 训练增强、类别平衡与 M2 临时旁路

训练图像增强集中在 `idv_agent/training/image_augmentation.py`：每个 history window 共享一组
色相 ±0.05、饱和度/亮度/对比度 0.8～1.2、平移 ±8 px 参数；验证集和原始数据不增强。
增强路径禁止使用持久视觉特征缓存，避免随机变换结果污染缓存或跨窗口复用。

move、camera dx、camera dy、intent 四个 head 使用训练子集全局计数构造 inverse-sqrt 类别权重，
各 head 独立归一化均值为 1，并 clamp 到 `[0.35, 3.0]`。权重表、计数、schema、源路径和 UTC
统计时间写入 checkpoint manifest。训练 metrics 记录 per-class recall、camera 0 桶误转率；有基线时
额外检查稀有类 recall 至少提升 0.05、多数类下降不超过 0.05、0 桶误转率不超过 15%。

由于 M2_VG 当前存在问题，ACT 训练、离线评估和实时部署统一从基础 Qwen 初始化，禁止加载 M2；
manifest 使用 `initialization=base_without_m2` 明确记录该临时协议，旧 M2 结果不与新 checkpoint
直接比较。旧诊断参数保留仅为命令兼容，不参与生产模型加载。

## 2026-09-04 · k=8 深层空间信号硬验收

短训练（30～60 步）现在记录 `gradient_norm_history`，并可通过 `--require-training-behavior` 强制要求
总 loss 下降、所有 loss 有限、各头 loss 有限且梯度范数不超过 100。训练结果写入
`metrics.json.training_behavior_acceptance`，不是只看最后一个总 loss。

新增 `python -m idv_agent.scripts.evaluate_feature_activity`。工具以验证 JSONL 的真实 8 帧窗口，走与
部署完全相同的 slow→fast 条件链，测量最终 `fast_temporal` GRU 状态及 move/camera/button/intent logits：
同帧二次编码 cosine 必须至少 0.999；不同样本窗口的决策特征 cosine 必须小于 0.99；分别遮挡每帧左半和
右半后，最终 GRU 特征 L2 与动作 logits L1 均必须大于 `1e-4`。任一失败 CLI 返回 2，不能把 k=8 空间
聚合链视为有效。验证不启用训练增强，也不使用跨条件特征缓存。

## 2026-09-04 · FP16 首步溢出诊断与训练行为验收

**复现：** 在同一 v5 MVP 子集、seed=0、首步 loss=5.3632 的条件下，PyTorch
`GradScaler` 默认初始 scale=65536 会使反标度后的多个可训练模块梯度同时非有限；
异常传播到 visual projection、SpatialAgg、FiLM、两路 GRU 和部分 MLP，随后全局梯度裁剪
只能看到 NaN，无法定位来源。将初始 scale 改为 1024 后，同一首步梯度范数为 6.220，
所有已命名可训练参数梯度均有限。

**修复：** `train_vla.py` 新增 `_make_grad_scaler()` 和 `--grad-initial-scale`，默认
`1024`；在反标度、裁剪之前记录每步非有限梯度参数名、dtype 和最大绝对值，并把完整
`metrics.json` 写盘后再执行 `--require-training-behavior` 硬门禁。这样失败实验仍保留可审计证据。

**实际验收：** 30 步 strict run 的 loss 从 `8.9009` 降至 `3.2274`，梯度全有限，最大范数
`27.45`，训练行为门禁通过。该 checkpoint 的 k=8 深层特征门禁仍失败：不同场景 cosine
`1.000`，左右遮挡最终 GRU 特征 L2 `2.29e-6`、输出 L1 `0`；四组画面依赖也失败，
`normal/image_zero/image_shuffle/history_zero` 的 move/camera/intent 指标完全相同。
因此它只证明训练数值稳定，不得作为视觉策略模型或进入部署。

## 2026-09-04 · Spatial-cell ACT 实测：数值收敛但画面依赖仍未成立

**改动：** ACT 不再把 `[64,1024]` 视觉 cell token 先聚合为一个向量；改为每胞共享
`LayerNorm → Linear(1024,32) → GELU`、加位置参数、固定 raster 展平并融合为 2560 维帧协议。
`SharedTemporalEncoder` 还加入当前有效帧的可训练 residual，checkpoint 只保存
`spatial_cell_projector + condition_projection`，并 fail-closed 拒绝旧 `SpatialAgg` ACT checkpoint。
训练时按样本清空完整 72 维 history（p=0.5）；验证和部署保持固定 v5 输入协议，不应用 dropout。

**实测训练：** MVP v5 train 的 32 个 stratified chunks，batch=4、lr=`1e-4`、30 步、FP16 +
GradScaler、训练图像增强和有界 inverse-sqrt 类别权重开启。loss `7.1781 → 5.0594`，所有梯度有限，
训练行为门禁通过，峰值显存 10,415 MB。此前 batch=1、lr=`1e-3` 的同配置在 slow loss 上出现尖峰、
裁剪前梯度峰值 230.89，严格门禁拒绝；不能以梯度裁剪掩盖该不稳定实验。

**硬验收结果（失败）：**
- 深层特征：同帧 cosine=`1.0`，左右遮挡的 fast temporal feature L2=`0.10460`、输出 L1=`7.50e-4`；
  但不同场景 cosine=`0.9999987`，未达到 `<0.99` 的空间信号生存门槛。
- 四组依赖（4 个留出样本）：normal 的 move/camera/intent 为 `0.375/0.34375/1.0`；image_zero 与
  image_shuffle 完全相同，三项 drop 均为 0，未达到每项至少 `max(0.05, normal×0.15)`；history_zero
  的 move 反而为 `0.4375`。

**结论：** cell 保序与 GRU 输入 residual 已让左右遮挡产生非零数值扰动，却尚未使跨场景决策表征
解共线或让输出依赖真实画面。该 checkpoint 绝不进入 dry-run/部署；下一步必须先进行 projector、FiLM、
fast GRU、head 的逐层方差/余弦诊断，并以 FiLM 解耦和 direct-to-head visual residual 的对照验证原因。

## 2026-09-04 · v5 去相关重建实测：消除标签重叠仍不足以让策略使用画面

**根因证据：** 原始 v5 的 `anchor_stride=12` 小于 action chunk 的未来 24 帧监督跨度；99 session 的
相邻 label horizon 因此全部重叠。复制最后一条 6 帧 macro history 的命中率为 move=80.7%、camera dx=66.4%、
camera dy=88.5%，模型可仅依赖动作先验取得低 CE。

**数据修复：** v5 默认 `anchor_stride` 改为 36；保持 8 帧/stride=3 观测、8 条 6 帧 macro history、30 FPS
和像素相机桶不变。99 session 重新构建为 598 chunks，逐条 schema/alignment/audit 通过，逐 session 的未来
监督窗口 overlap=0。新的 MVP 集 `data/mvp_vla_v5_decorrelated` 为 train=480（79 session）、val=118（20 session），
两侧都有 travel/decipher 且 session 零重叠。

**训练实测：** base Qwen（不加载 M2）、batch=4、lr=5e-5、30 step、FP16+GradScaler、history dropout=0.5、
训练增强和 head 内独立有界 inverse-sqrt 权重开启。loss `7.4502 → 3.4757`，最大梯度范数 `34.42`，训练行为门禁通过。
但留出预测仍偏多数类；camera dx/dy 的 0 桶误转率未达 <15% 目标。

**视觉硬验收（32 条固定分层 cohort）：** cohort 含 travel=28、decipher=4，并覆盖 camera dx 五桶及验证集中
存在的 dy 非零桶。normal move/camera/intent=`0.3281/0.5313/0.8750`；image_zero=`0.3281/0.5391/0.8750`；
image_shuffle=`0.3281/0.5352/0.8750`，三头均未达到所需下降。history_zero 的 camera 反而升至 `0.6172`。深层
feature 测试也仍有不同场景 cosine=`0.999890`（要求 <0.99），尽管左右遮挡 L2=`0.8537`、logit L1=`0.00562`。

**结论：** 采样去相关已排除标签窗口重叠这一条捷径，但没有建立“画面状态→动作”所需的同源监督；它不能单独
解决模型不用画面。该 checkpoint continue fail-closed，不能进入 MVP/dry-run/部署。下一阶段必须以当前 raw
session 的同源视觉状态/对象位置标注构建 ACT 联合辅助目标，并只把模型预测的视觉 embedding（绝不是真值 YOLO）
注入决策路径后重新接受同一四组门禁。
## 2026-09-04 · m8 raw-grid direct ACT 实测

**动机：** m7 的 `SpatialCellProjector` 将冻结的 `[64,1024]` 空间 token 压缩为 2560 维，虽有
history-free visual expert，仍出现不同场景 cosine=`0.999942`，且 image-zero/image-shuffle 输出不变。
这说明学习型 raster 压缩仍可能抹掉 frozen raw grid 中已被线性 probe 证明存在的视觉信号。

**改动：** `Qwen3VLBackboneAdapter.project_raw_features()` 现在只做确定性 8×8→2×2、4×4 邻域平均，
展平为 `act_feature_dim=4096`；Qwen 文本 `hidden_size=2560` 仅用于 instruction/mode condition projection，
不再用于构造 ACT core。训练、评估、诊断、实时 benchmark 全部按 `adapter.act_feature_dim` 建 core。
checkpoint schema 改为 `m8_act.raw_grid_direct.v1`，adapter state 只允许 condition projection，m7/M2/旧
SpatialAgg 状态 fail-closed。新增 raw pool、空间象限交换、core 维度、旧 schema 拒绝和 cache/online 一致性测试。

**验证：** 全量 pytest `226 passed`。MVP v5（train=480、val=118）60 步 FP16+GradScaler 训练 loss
`26.6174 → 1.3385`，无 NaN；32 条 held-out 分类评估 move=`0.6641`、camera dx=`0.5000`、dy=`0.7813`、
intent=`1.0`，button pressed recall=`0`。四组画面依赖中 normal/image_zero/image_shuffle 的
move/camera/intent 完全相同，gate 失败；深层不同场景 cosine=`0.999752`，但左右遮挡 feature L2=`31.0974`、
output L1=`0.45868`。

**结论：** 直接 raw-grid 已证明空间扰动进入网络，但 60 步训练仍被多数类/先验支配，尚未证明模型依赖真实
画面；该 checkpoint 继续 fail-closed，不进入 MVP、dry-run 或部署。不得通过继续延长训练掩盖 gate 失败，
后续应优先改视觉监督/输出校准与类别验收，再重训。

## 2026-09-05 · m12 纯视觉直连、公共方向中心化与平衡门禁

**定位：** m8/m9 的四条件指标不变，分支诊断发现 image-zero 会改变视觉特征，却不改变 visual logits 的
argmax；m10 去掉 bias 后仍受高维输入尺度影响。进一步确认 m7 的 visual expert 输入曾混入 episode 固定
condition，且 visual path 先经过 8192→256 隐层压缩。最终动作/意图还被普通 accuracy 的多数类效应误导：
黑图预测 camera 0/travel 时 accuracy 反而上升。

**改动：** m12 将 VisualActionExpert 改为直接读取纯 raw `[last, last-first]` 8192 维特征，去掉 hidden
bottleneck 和 visual 分类 bias；用训练集分层样本计算持久 input center，训练、评估、部署共用 checkpoint
中的 center。部署 `.fast/.slow` 严格等于 visual branch，temporal/history prior 仅保留为诊断输出；visual
branch 在 Turing FP16 autocast 下强制 FP32，避免 direct high-dimensional linear gradient overflow。
增加四条件逐头 logit delta/argmax flip-rate 与 branch accuracy 报告。

**门禁修复：** 在严重类别不平衡下，画面依赖门禁新增 observed-class macro recall（balanced move/camera/
intent），仍保留普通 accuracy；image-zero/image-shuffle 的比较优先使用 balanced 指标，防止多数类准确率掩盖
rare class 完全丢失。

**实测：** m12 batch=4、完整 480 train chunks、480 steps、lr=`1e-4`、训练增强、每头 clamp `[0.35,3.0]`
类别权重、history dropout=`0.5`：loss `7.4318 → 4.0332`，最大梯度 `77.27`，全有限。32 条 held-out
结果 balanced normal/image-zero/image-shuffle：move=`0.4984/0.1667/0.2599`、camera=`0.1953/0.2250/0.1865`、
intent=`0.8929/0.5000/0.7500`；move/intent 的画面依赖成立，camera 未成立。逐头 logit 翻转表明 image-zero
仍触发 move=`0.547`、dx=`0.695`、dy=`0.367`、intent=`0.313`，image-shuffle 触发
move=`0.656`、dx=`0.719`、dy=`0.578`、intent=`0.438`。

**结论：** m12 已解决“模型完全不用画面”的结构问题，且在 move/intent 上通过了平衡画面贡献验证；camera
仍接近随机，说明未来 camera action 与当前画面之间没有足够的同源监督，不能靠继续调 loss 或训练步数保证。
必须补充 camera 目标对应的视觉状态/目标位置辅助标注，或重定义 camera 监督为可由画面预测的状态转向目标。
m12 checkpoint 继续 fail-closed，不进入 MVP、dry-run 或部署。

## 2026-09-05 · m13 camera prior 对照

**问题：** m12 已让 move/intent 的 balanced 画面贡献成立，但 camera balanced recall 约为随机水平；
这与 camera 是当前观察帧之后的鼠标动作、且历史 camera 动作具有明显自相关有关。

**对照：** m13 只给 camera dx/dy 加 `0.1*tanh(prior_logits)` 的 history/temporal prior，move、intent、button
仍保持 visual-only；prior 受硬上限约束并写入 schema `m13_act.visual_camera_prior.v1`。在现有 m13 120 步
全量训练与离线诊断中，camera balanced normal=`0.1607`，image-zero=`0.2250`，image-shuffle=`0.1824`；
普通 accuracy 的黑图上升仍由多数类造成，camera prior 未改善画面依赖，也不能绕过 camera gate。

**结论：** 现有 camera 标签缺少“当前画面状态→需要怎样转镜头”的稳定监督。下一步应在动作块中增加
画面可见目标/朝向/目标相对位置等同源辅助目标，或重新定义 camera action 为可由当前画面预测的状态误差；
继续单纯放大 prior 或训练步数没有证据支持。

训练集补充统计：上一条 6 帧 history camera 桶直接复用到当前 action chunk 的联合命中率为 `57.2%`，
高于视觉 camera 头，说明 camera 的主要可预测来源是行为延续而非当前画面。m14 将 camera prior 从 `0.1`
提至 `0.5` 后，32 条 held-out 的 balanced camera normal/image-zero/image-shuffle 为
`0.2097/0.2250/0.2217`，仍未通过，因此不能用 history prior 冒充画面贡献。

## 2026-09-05 · Q 交互脉冲语义修复

**问题：** 原始 session 中 100 次 Q 按下/抬起被状态机识别为持续破译状态，但
v5 构建器把所有 `INTERACT_HOLD` 帧都转换为 `buttons[0]=1`。结果单次 Q 被扩展为
最长 76 帧，部署执行器又把该位当作 held key，无法准确表达“出现交互 UI 后按一次 Q
进入自动破译”。

**修复：** `DecodeStatus` 增加单帧 `started` 边沿，逐帧产物增加 `decode_start`；
v5 宏动作只在包含该边沿的 6 帧窗口写入一次 `buttons[0]=1`，后续破译状态保持
`INTERACT_HOLD` 语义但不再产生 Q 按键。`ACTActionChunkExecutor` 将交互位发送为
一次 `press + release`，并排除出持续 held 集合。camera 仍严格来自同一 v5 Raw Input
6 帧净位移和共享像素桶/执行查表。

**数据重建：** 原始 `data/new_vla_raw_sessions` 的 99 个 session 已重新构建 v5，
输出共 598 chunks，全部单 session audit 通过；全局得到 70 个唯一交互脉冲、0 个
重复脉冲 chunk。旧 v5 MVP 集未覆盖，保留为对照；后续 stride24 修复后的数据集见
下方冷启动与窗口覆盖记录。

## 2026-09-05 · v5 冷启动与未来窗口覆盖修复

**问题：** v5 原默认 `anchor_stride=36`，而四步动作块只覆盖 24 帧，窗口之间留下
12 帧空洞。全量 100 次 Q 事件中仅 69 次落入训练 action chunk；提前首个 anchor
只能覆盖开局边界，不能覆盖中间空洞。

**修复：** canonical v5 默认 `anchor_stride=24`，与 `4×6` 帧未来标签窗口完全相等；
相邻监督窗口连续且无重叠。首个 anchor 在视觉窗口预热的第 21 帧开始，缺失的 8 条
已执行宏动作 history 用显式零动作填充，训练与部署的冷启动输入协议一致。全量 raw
session 仍使用真实 Raw Input camera 复刻标签。最终 99 个 session 产出 983 chunks，
原始 100 次 Q 全部落入 action window，交互脉冲 100 个且无重复；MVP 集
`data/mvp_vla_v5_replay_pulse_coldstart_stride24` 为 train=787（79 sessions）、
val=196（20 sessions），session 零重叠。

## 2026-09-06 · Q 决策标签对齐与 m20/m21 本机验收

**根因：** Q event-centered 样本用 `anchor=q-2` 保留交互 UI 出现前后的因果观测，未来
动作从 `q` 开始；但顶层 `slow_label` 原先仍按 anchor 读取。实际 100 个 Q 边沿中，98 个
处于 `decipher` 段，另有 2 个因人工段滞后仍落在 `travel` 段，因此旧监督会教模型
`travel → Q`，而非目标行为 `decipher/start_decoding → Q`。

**修复：** event-centered 记录的观测帧标签保持真实历史帧语义；记录顶层决策标签改取
`action_start_frame=q`。有 `decode_start=1` 时，事件是进入自动破译的权威证据，固定为
`decipher/start_decoding`；`auxiliary` 同时写入 `decision_label_frame`、
`decision_label_source=decode_start_event` 和原始 `source_segment_id` 以审计人工段冲突。

**数据：** 新实验集为 `data/mvp_vla_v5_event_centered_decision_label_v2`：train=732
（79 sessions）、val=182（20 sessions），session 零重叠。100 个 Q 事件均保留（79/20）且
均监督为 `decipher`；慢标签 train=`travel/decipher=627/105`，val=`157/25`。

**m19：** 拆分 Q 为独立 `interact_event` 头，普通五个持续按钮另有独立负先验；camera
logits 融合完整 visual pair 与视觉轨迹摘要。checkpoint schema 为
`m19_act.calibrated_event_camera_pair.v1`，旧权重 fail-closed。事件阈值仅用训练集拟合并被
checkpoint、离线评估、实时 benchmark、ACTPolicy 同源消费。

**本机训练与验收：** 均使用 2080 Ti、`idv312`、FP16+GradScaler、base Qwen（M2 不加载）、
batch=4、lr=1e-4、增强、history dropout=0.5、每头 bounded inverse-sqrt 权重。m20=366 步
（约 2 epoch），m21=732 步（约 4 epoch）；均无 NaN/非有限梯度。m21 的训练集 Q 阈值校准
recall=94.94%、逐步 FPR=1.83%；k=8 门禁通过：不同场景 cosine=0.05697、左右遮挡
feature L2=64.24、输出 L1=0.9417。

**不放行结论：** m21 的完整 182 条 session-held-out 四组门禁仍为
`visual_dependency_gate_pass=false`。normal balanced move/camera/intent 为
`0.34696/0.27017/0.88357`；image-zero 时 camera drop=0.04810，小于 0.05；image-shuffle
时 move drop=0.03519，小于 0.05204，camera drop=0.04840，小于 0.05；intent 两组通过。
所有 m18/m19/m20/m21 checkpoint 均不可进入 dry-run 或真实发送。

**下一个数据依赖：** camera 的 Raw Input 复刻标签没有“画面中应该朝哪里转”的同源目标。
继续加步数从 2 到 4 epoch 未解决该门禁。需人工补标：密码机 bbox/中心、是否交互可达、目标在
角色视角的相对侧；优先 100 个 Q 前后窗口与 travel 的非零 camera 窗口。标注只能作训练期
辅助目标，部署只使用模型预测视觉表示，不读取真值或外部规则。

## 2026-09-06 · 同 session ACT 视觉 grounding 标注池

**更正：** 对 event-centered 决策标签 v2 的 canonical JSONL 按 `action_chunk[*].buttons[0]`
逐步重算，实际有 99 个 Q 事件（train=79，val=20），跨 98 个 session；验证局
`20260903_102739_228371` 包含两次。此前“100”来自冷启动 stride24 的前一版数据，不能用于
本版 m21 的标注覆盖统计。

**产物：** `data/mvp_act_grounding_pool_v1` 从 canonical 数据集导出 1,380 个原帧副本
（train=1,076，val=304），保存 `manifest.jsonl` 和 `audit.json`；原始帧、raw session 与
canonical JSONL 均不改写。每个 Q 动作执行帧的 `±6` 帧上下文均被选入，并为 train/val 各自的
非零 `camera_dx/dy` 桶抽取最多 8 个确定性代表决策帧。每条 manifest 记录来源帧、split、以及
`q_window` 或 `camera_nonzero` 的选择理由，禁止 train/val session 泄漏。

**标注契约：** bbox 继续只维护为现有三类 YOLO 框；不可由 bbox 表达的
`cipher_reachable`、`target_side`、`interact_prompt` 位于独立的
`act_grounding_annotations.jsonl`。校验器拒绝未填字段、重复/遗漏 id、无框但声称可达，或有框却
没有有效方向。它不会把未标帧悄悄当作负样本。标注是真正训练期辅助监督，部署 ACT 只消费模型的
图像特征；不得把人工框或该 sidecar 拼入实时输入。

**2026-09-06 标注导入：** 用户确认 `mvp_act_grounding_pool_v1/images` 的 train/val 标注已经
完成，并确认没有 X-AnyLabeling sidecar 的图片代表“密码机不可见”。786 份 JSON 经过现有转换器
得到 1,431 个框（`cipher_visible=714`、`cipher_highlight=55`、`interact_prompt=662`），其余 594
张作为空 YOLO 标签。自动辅助标注完整覆盖 1,380 帧（train=1,076、val=304）：bbox=761、
无可见密码机=619、Q 提示/可达=662。训练中有 25 帧为角色遮挡密码机、但 UI 仍提示 Q：保留
`reachable=1/prompt=1/bbox=null/side=none`，不臆造框；验证集中无此组合。数据已通过完整性校验，
下步可接入 ACT 视觉辅助头，仍须以四组画面依赖门禁决定是否放行。

**2026-09-06 重标注契约：** 先前“Q 提示可在机器完全遮挡、无密码机框时成立”的假设被用户否定。
重新标注规定：`interact_prompt` 出现时密码机一定可见，必须同时有 `cipher_visible` 或
`cipher_highlight` 框；`target_side` 由 `interact_prompt` 框中心而非密码机框中心决定。旧派生
`labels/` 和辅助 JSONL 已在 `data/mvp_act_grounding_pool_v1` 留存带时间戳的可恢复备份，随后从
1,363 个新 X-AnyLabeling JSON 重建 2,562 个框：visible=1,304、highlight=72、prompt=1,186；
剩余无 JSON 的 17 帧按用户确认标为无可见密码机。新辅助标签 1,380 帧完整校验通过，prompt→
密码机框/可达性硬约束违规为 0。

## 2026-09-06 · m22 同帧视觉 grounding 辅助训练

**目的：** Raw Input 相机回放只监督“人转了多少”，不足以监督“画面中目标在哪里”。m22 在
ACT 的 history-free `VisualActionExpert` 直接全窗口视觉 pair 特征上，增加训练期
`cipher_present`、`bbox_xyxy_norm`、`target_side`、`interact_prompt`、`cipher_reachable` 五个
辅助头；不接在压缩 GRU、历史动作、slow condition、YOLO 推理或规则输出后。

**因果和数据边界：** 标注通过 `(episode_id, alignment.observation_end_frame)` 对齐最后一张
真实观测帧，绝不读取 `action_start_frame` 或未来帧。当前 MVP chunks 中 train=128/732、val=64/182
有该同帧人工监督；其余样本 `grounding_mask=0`，不被伪造为负样本。bbox loss 仅在可见密码机框
存在时计算。部署模型仍只接收图像、指令、模式和既定 history，绝不读取标注 JSONL 或 YOLO。

**门禁：** checkpoint schema 升级为 `m22_act.grounded_visual_pair.v1`，旧 m19/m21 权重
fail-closed。验证除了 ACT 四组画面贡献门禁外，另报告仅在 `grounding_mask=1` 样本上的
presence/prompt/reachable、side accuracy 和 bbox L1；未标注 chunk 不参与这些指标。
